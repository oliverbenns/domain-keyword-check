# Domain Keyword Checker

Checks .com domain availability with a combination of 2 words.

## Running

-   Update `data.json` with your preferred words.
-   `docker compose up -d` (for redis to cache)
-   `go run .`
